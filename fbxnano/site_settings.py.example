"""Site- or environment-specific Django settings

Use this file to configure or override settings that are specific ro one site
or environment, such as turning on DEBUG for development. This is also where
ALL "secret" settings should live, such as SECRET_KEY and database passwords.

Best use for this file is to `import *` at the end of your settings.py
"""

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = ''

# Turn debugging on in dev
#DEBUG = True
#TEMPLATE_DEBUG = True

# Allow all hosts in dev
#ALLOWED_HOSTS = ['*']

# Database configuration
#DATABASES = {
#    'default': {
#        'ENGINE': 'django.db.backends.postgresql_psycopg2',
#        'NAME': 'database_name',
#    }
#}
